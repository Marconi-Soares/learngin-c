#include <stdio.h>
#include <string.h>

int main(void){
    char string1[50] = "Marconi Soares";
    char string2[] = "Marconi SoarEs";
    
    // strcpy(string1, string2);
    // strncat(string1, string2, 2);
    // memset(string1, '?', sizeof( string1 ));
    // short res = strcasecmp(string1, string2);
    // printf("%hd\n", res);
    return 0;
}
